<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esteban.santos">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAf6liK9lU8X4xH6Avv5od2X9f3bJeZtNs" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_menu" />
        <activity
            android:name=".FireMenu"
            android:label="@string/title_fire_menu" />
        <activity android:name=".LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StormMenu"
            android:label="@string/title_storm_menu" />
        <activity
            android:name=".MedicalMenu"
            android:label="@string/title_medical_menu" />
        <activity
            android:name=".SecurityMenu"
            android:label="@string/title_security_menu" />
        <activity
            android:name=".FireHotlines"
            android:label="@string/title_fire_hotlines" />
        <activity
            android:name=".FireGuidelines"
            android:label="@string/title_fire_guidelines" />
        <activity
            android:name=".FireFirstAid"
            android:label="@string/title_fire_firstaid" />
        <activity android:name=".StormHotlines" />
        <activity android:name=".StormGuidelines" />
        <activity android:name=".StormFirstAid"></activity>
    </application>

</manifest>